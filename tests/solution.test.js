var func = require('../solution');

var assert = require('assert');
describe('Find Smaller tests', () => {
  it('should return -1', () => {
    var result = func(100);
    assert.equal(result, -1);
  });
});
